# Packaging

| Thư mục | Nội dung |
|---------|----------|
| [`linux/`](linux/) | `.desktop`, `cpack_deb.sh`, `sync_cmake_version.py` — CPack `.deb` |
| [`windows/`](windows/) | `build_installer.ps1`, QTIFW `installer_build/`, `convert_tool/`, hướng dẫn |

Cấu hình CPack chung: [`cmake/CPackOptions.cmake`](../cmake/CPackOptions.cmake).
